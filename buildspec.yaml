version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
       - aws cloudformation package --template-file template.yaml --output-template-file outputsamTemplate.yaml
artifacts: 
  type: zip 
  files:
    - template.yaml
    - outputsamTemplate.yaml 
