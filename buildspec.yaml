version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
       - aws cloudformation package --template-file template.yaml --s3-bucket codepipeline-eu-west-2-582809705180 --output-template-file outputsamTemplate.yaml
artifacts: 
  type: zip 
  files:
    - template.yaml
    - outputsamTemplate.yaml 
